const hasLicence = true;
const age = 21;
const isDrunk = false;

if (hasLicence && age >= 18 && !isDrunk) {
    console.log('Может вести машину');   
} else {
    console.log('Нельзя вести машину');
}